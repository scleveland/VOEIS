$("#<%= @tabId %>_search_results").html( '<%= escape_javascript(render(:partial => "show_query_results",
                                      :locals => {:tabId => @tabId,
                                      :site => @site_name,
                                      :project_uid => @project_uid,
                                      :variable => @var_name,
                                      :variable_data => @variable,
                                      :site_data => @site,
                                      :units => @units,
                                      :start_date => @start_date,
                                      :end_date => @end_date,
                                      :data_structs =>@data_structs,
                                      :graph_data => @graph_data,
                                      :data_set_options => @data_set_options,
                                       }))%>' )

dijit.byId('<%= @tabId %>_loading_dialog').hide();



